fastapi>=0.115.0
uvicorn>=0.30.0
requests>=2.31.0

# Only needed when MUSIC_SOURCE=youtube. The default Audius source does not use
# these. YouTube breaks older yt-dlp releases regularly, so keep this current.
ytmusicapi>=1.10.0
yt-dlp>=2026.8.19
